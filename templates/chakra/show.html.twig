{% extends 'base.html.twig' %}

{% block title %}Chakra{% endblock %}

{% block body %}
    <h1>Chakra</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chakra.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ chakra.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chakra_index') }}">back to list</a>

    <a href="{{ path('app_chakra_edit', {'id': chakra.id}) }}">edit</a>

    {{ include('chakra/_delete_form.html.twig') }}
{% endblock %}
