<form method="post" action="{{ path('app_choix_bio_specifique_delete', {'id': choix_bio_specifique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ choix_bio_specifique.id) }}">
    <button class="btn">Delete</button>
</form>
