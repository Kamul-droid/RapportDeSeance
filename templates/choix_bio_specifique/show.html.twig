{% extends 'base.html.twig' %}

{% block title %}ChoixBioSpecifique{% endblock %}

{% block body %}
    <h1>ChoixBioSpecifique</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ choix_bio_specifique.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ choix_bio_specifique.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_choix_bio_specifique_index') }}">back to list</a>

    <a href="{{ path('app_choix_bio_specifique_edit', {'id': choix_bio_specifique.id}) }}">edit</a>

    {{ include('choix_bio_specifique/_delete_form.html.twig') }}
{% endblock %}
