<form method="post" action="{{ path('app_profil_cerveau_delete', {'id': profil_cerveau.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profil_cerveau.id) }}">
    <button class="btn">Delete</button>
</form>
