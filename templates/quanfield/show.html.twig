{% extends 'base.html.twig' %}

{% block title %}Quanfield{% endblock %}

{% block body %}
    <h1>Quanfield</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quanfield.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ quanfield.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_quanfield_index') }}">back to list</a>

    <a href="{{ path('app_quanfield_edit', {'id': quanfield.id}) }}">edit</a>

    {{ include('quanfield/_delete_form.html.twig') }}
{% endblock %}
