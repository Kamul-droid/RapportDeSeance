<form method="post" action="{{ path('app_facteurs_de_stress_conflictuel_delete', {'id': facteurs_de_stress_conflictuel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facteurs_de_stress_conflictuel.id) }}">
    <button class="btn">Delete</button>
</form>
