<form method="post" action="{{ path('app_primary_object_delete', {'id': primary_object.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ primary_object.id) }}">
    <button class="btn">Delete</button>
</form>
