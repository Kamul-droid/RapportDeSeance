{% extends 'base.html.twig' %}

{% block title %}PrimaryObject{% endblock %}

{% block body %}
    <h1>PrimaryObject</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ primary_object.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ primary_object.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_primary_object_index') }}">back to list</a>

    <a href="{{ path('app_primary_object_edit', {'id': primary_object.id}) }}">edit</a>

    {{ include('primary_object/_delete_form.html.twig') }}
{% endblock %}
