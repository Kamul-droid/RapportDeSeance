{% extends 'base.html.twig' %}

{% block title %}GlobalDescription{% endblock %}

{% block body %}
    <h1>GlobalDescription</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ global_description.id }}</td>
            </tr>
            <tr>
                <th>Subject</th>
                <td>{{ global_description.subject }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_global_description_index') }}">back to list</a>

    <a href="{{ path('app_global_description_edit', {'id': global_description.id}) }}">edit</a>

    {{ include('global_description/_delete_form.html.twig') }}
{% endblock %}
