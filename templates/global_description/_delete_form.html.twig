<form method="post" action="{{ path('app_global_description_delete', {'id': global_description.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ global_description.id) }}">
    <button class="btn">Delete</button>
</form>
