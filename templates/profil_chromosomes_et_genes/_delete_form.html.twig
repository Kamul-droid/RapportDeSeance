<form method="post" action="{{ path('app_profil_chromosomes_et_genes_delete', {'id': profil_chromosomes_et_gene.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profil_chromosomes_et_gene.id) }}">
    <button class="btn">Delete</button>
</form>
