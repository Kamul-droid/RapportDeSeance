<form method="post" action="{{ path('app_profil_bio_feed_back_gemmes_delete', {'id': profil_bio_feed_back_gemme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profil_bio_feed_back_gemme.id) }}">
    <button class="btn">Delete</button>
</form>
