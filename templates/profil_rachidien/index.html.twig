{% extends 'base.html.twig' %}

{% block title %}ProfilRachidien index{% endblock %}

{% block body %}
    <h1>ProfilRachidien index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for profil_rachidien in profil_rachidiens %}
            <tr>
                <td>{{ profil_rachidien.id }}</td>
                <td>{{ profil_rachidien.nom }}</td>
                <td>
                    <a href="{{ path('app_profil_rachidien_show', {'id': profil_rachidien.id}) }}">show</a>
                    <a href="{{ path('app_profil_rachidien_edit', {'id': profil_rachidien.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_profil_rachidien_new') }}">Create new</a>
{% endblock %}
