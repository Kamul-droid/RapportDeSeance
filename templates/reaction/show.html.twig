{% extends 'base.html.twig' %}

{% block title %}ReactionAge{% endblock %}

{% block body %}
    <h1>ReactionAge</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reaction_age.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ reaction_age.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reaction_index') }}">back to list</a>

    <a href="{{ path('app_reaction_edit', {'id': reaction_age.id}) }}">edit</a>

    {{ include('reaction/_delete_form.html.twig') }}
{% endblock %}
