{% extends 'base.html.twig' %}

{% block title %}ReactionAge index{% endblock %}

{% block body %}
    <h1>ReactionAge index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for reaction_age in reaction_ages %}
            <tr>
                <td>{{ reaction_age.id }}</td>
                <td>{{ reaction_age.nom }}</td>
                <td>
                    <a href="{{ path('app_reaction_show', {'id': reaction_age.id}) }}">show</a>
                    <a href="{{ path('app_reaction_edit', {'id': reaction_age.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_reaction_new') }}">Create new</a>
{% endblock %}
