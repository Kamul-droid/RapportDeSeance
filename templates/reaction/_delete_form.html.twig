<form method="post" action="{{ path('app_reaction_delete', {'id': reaction_age.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reaction_age.id) }}">
    <button class="btn">Delete</button>
</form>
