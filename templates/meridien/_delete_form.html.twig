<form method="post" action="{{ path('app_meridien_delete', {'id': meridien.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ meridien.id) }}">
    <button class="btn">Delete</button>
</form>
