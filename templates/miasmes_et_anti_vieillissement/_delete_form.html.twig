<form method="post" action="{{ path('app_miasmes_et_anti_vieillissement_delete', {'id': miasmes_et_anti_vieillissement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ miasmes_et_anti_vieillissement.id) }}">
    <button class="btn">Delete</button>
</form>
