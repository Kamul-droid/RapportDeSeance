{% extends 'base.html.twig' %}

{% block title %}Feedbacksupplementaire{% endblock %}

{% block body %}
    <h1>Feedbacksupplementaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ feedbacksupplementaire.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ feedbacksupplementaire.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_feedbacksupplementaire_index') }}">back to list</a>

    <a href="{{ path('app_feedbacksupplementaire_edit', {'id': feedbacksupplementaire.id}) }}">edit</a>

    {{ include('feedbacksupplementaire/_delete_form.html.twig') }}
{% endblock %}
