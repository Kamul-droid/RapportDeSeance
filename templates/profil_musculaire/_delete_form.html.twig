<form method="post" action="{{ path('app_profil_musculaire_delete', {'id': profil_musculaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profil_musculaire.id) }}">
    <button class="btn">Delete</button>
</form>
