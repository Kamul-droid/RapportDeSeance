{% extends 'base.html.twig' %}

{% block title %}ProfilMusculaire index{% endblock %}

{% block body %}
    <h1>ProfilMusculaire index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Transfert</th>
                <th>Choix_bio_specifique</th>
                <th>Choix_items</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for profil_musculaire in profil_musculaires %}
            <tr>
                <td>{{ profil_musculaire.id }}</td>
                <td>{{ profil_musculaire.transfert }}</td>
                <td>{{ profil_musculaire.choixBioSpecifique }}</td>
                <td>{{ profil_musculaire.choixItems }}</td>
                <td>
                    <a href="{{ path('app_profil_musculaire_show', {'id': profil_musculaire.id}) }}">show</a>
                    <a href="{{ path('app_profil_musculaire_edit', {'id': profil_musculaire.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_profil_musculaire_new') }}">Create new</a>
{% endblock %}
