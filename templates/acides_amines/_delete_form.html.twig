<form method="post" action="{{ path('app_acides_amines_delete', {'id': acides_amine.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ acides_amine.id) }}">
    <button class="btn">Delete</button>
</form>
