{% extends 'base.html.twig' %}

{% block title %}Acides Amines {% endblock %}


{% block monTitre %}<div class="fst-italic"> Acides Aminés </div>{% endblock %}

{% block body %}
    <table class="table-sm table-responsive col-md-4 offset-4 table-bordered mg border border-5 border-info rounded-5  " >

        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for acides_amine in acides_amines %}
            <tr>
                <td>{{ acides_amine.id }}</td>
                <td>{{ acides_amine.nom }}</td>
                <td>
                    <a href="{{ path('app_acides_amines_show', {'id': acides_amine.id}) }}">show</a>
                    <a href="{{ path('app_acides_amines_edit', {'id': acides_amine.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div class="text-center"><a class=" btn btn-warning" href="{{ path('app_acides_amines_new') }}">Ajouter Meridien</a></div>

{% endblock %}
