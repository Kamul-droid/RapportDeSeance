<form method="post" action="{{ path('app_quantareport_delete', {'id': quantareport.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quantareport.id) }}">
    <button class="btn">Delete</button>
</form>
