<form method="post" action="{{ path('app_animaux_familles_delete', {'id': animaux_famille.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ animaux_famille.id) }}">
    <button class="btn">Delete</button>
</form>
