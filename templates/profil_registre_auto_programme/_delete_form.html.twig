<form method="post" action="{{ path('app_profil_registre_auto_programme_delete', {'id': profil_registre_auto_programme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profil_registre_auto_programme.id) }}">
    <button class="btn">Delete</button>
</form>
