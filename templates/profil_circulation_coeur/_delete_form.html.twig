<form method="post" action="{{ path('app_profil_circulation_coeur_delete', {'id': profil_circulation_coeur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profil_circulation_coeur.id) }}">
    <button class="btn">Delete</button>
</form>
