{% extends 'base.html.twig' %}

{% block title %}ProfilDentaire{% endblock %}

{% block body %}
    <h1>ProfilDentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profil_dentaire.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ profil_dentaire.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_profil_dentaire_index') }}">back to list</a>

    <a href="{{ path('app_profil_dentaire_edit', {'id': profil_dentaire.id}) }}">edit</a>

    {{ include('profil_dentaire/_delete_form.html.twig') }}
{% endblock %}
