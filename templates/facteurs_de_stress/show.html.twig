{% extends 'base.html.twig' %}

{% block title %}FacteursDeStress{% endblock %}

{% block body %}
    <h1>FacteursDeStress</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ facteurs_de_stress.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ facteurs_de_stress.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_facteurs_de_stress_index') }}">back to list</a>

    <a href="{{ path('app_facteurs_de_stress_edit', {'id': facteurs_de_stress.id}) }}">edit</a>

    {{ include('facteurs_de_stress/_delete_form.html.twig') }}
{% endblock %}
