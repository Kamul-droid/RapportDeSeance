<form method="post" action="{{ path('app_registre_de_susceptibilite_delete', {'id': registre_de_susceptibilite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ registre_de_susceptibilite.id) }}">
    <button class="btn">Delete</button>
</form>
