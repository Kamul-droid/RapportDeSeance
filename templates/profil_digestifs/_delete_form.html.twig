<form method="post" action="{{ path('app_profil_digestifs_delete', {'id': profil_digestif.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profil_digestif.id) }}">
    <button class="btn">Delete</button>
</form>
