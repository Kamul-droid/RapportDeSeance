{% extends 'base.html.twig' %}

{% block title %}ProfilNutrition{% endblock %}

{% block body %}
    <h1>ProfilNutrition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profil_nutrition.id }}</td>
            </tr>
            <tr>
                <th>Mineraux</th>
                <td>{{ profil_nutrition.mineraux }}</td>
            </tr>
            <tr>
                <th>Acides_amines</th>
                <td>{{ profil_nutrition.acidesAmines }}</td>
            </tr>
            <tr>
                <th>Vitamines</th>
                <td>{{ profil_nutrition.vitamines }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_profil_nutrition_index') }}">back to list</a>

    <a href="{{ path('app_profil_nutrition_edit', {'id': profil_nutrition.id}) }}">edit</a>

    {{ include('profil_nutrition/_delete_form.html.twig') }}
{% endblock %}
