<form method="post" action="{{ path('app_choix_solution_delete', {'id': choix_solution.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ choix_solution.id) }}">
    <button class="btn">Delete</button>
</form>
