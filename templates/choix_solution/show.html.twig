{% extends 'base.html.twig' %}

{% block title %}ChoixSolution{% endblock %}

{% block body %}
    <h1>ChoixSolution</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ choix_solution.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ choix_solution.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_choix_solution_index') }}">back to list</a>

    <a href="{{ path('app_choix_solution_edit', {'id': choix_solution.id}) }}">edit</a>

    {{ include('choix_solution/_delete_form.html.twig') }}
{% endblock %}
