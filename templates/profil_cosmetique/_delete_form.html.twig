<form method="post" action="{{ path('app_profil_cosmetique_delete', {'id': profil_cosmetique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profil_cosmetique.id) }}">
    <button class="btn">Delete</button>
</form>
