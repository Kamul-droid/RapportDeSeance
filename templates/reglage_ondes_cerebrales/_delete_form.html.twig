<form method="post" action="{{ path('app_reglage_ondes_cerebrales_delete', {'id': reglage_ondes_cerebrale.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reglage_ondes_cerebrale.id) }}">
    <button class="btn">Delete</button>
</form>
