{% extends 'base.html.twig' %}

{% block title %}HealthHistory{% endblock %}

{% block body %}
    <h1>HealthHistory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ health_history.id }}</td>
            </tr>
            <tr>
                <th>Descript</th>
                <td>{{ health_history.descript }}</td>
            </tr>
            <tr>
                <th>Illness</th>
                <td>{{ health_history.illness }}</td>
            </tr>
            <tr>
                <th>Event</th>
                <td>{{ health_history.event ? health_history.event|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_health_history_index') }}">back to list</a>

    <a href="{{ path('app_health_history_edit', {'id': health_history.id}) }}">edit</a>

    {{ include('health_history/_delete_form.html.twig') }}
{% endblock %}
