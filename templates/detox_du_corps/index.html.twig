{% extends 'base.html.twig' %}

{% block title %}Detox Du Corps{% endblock %}

{% block monTitre %}<div class="fst-italic"> Detox Du Corps </div>{% endblock %}

{% block body %}
<table class="table-sm table-responsive col-md-4 offset-4 table-bordered mg border border-5 border-info rounded-5">
    <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for detox_du_corp in detox_du_corps %}
            <tr>
                <td>{{ detox_du_corp.id }}</td>
                <td>{{ detox_du_corp.nom }}</td>
                <td>
                    <a href="{{ path('app_detox_du_corps_show', {'id': detox_du_corp.id}) }}">show</a>
                    <a href="{{ path('app_detox_du_corps_edit', {'id': detox_du_corp.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="text-center"><a class=" btn btn-warning" href="{{ path('app_detox_du_corps_new') }}">Ajouter Detox</a></div>
{% endblock %}
