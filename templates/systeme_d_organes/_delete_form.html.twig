<form method="post" action="{{ path('app_systeme_d_organes_delete', {'id': systeme_d_organe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ systeme_d_organe.id) }}">
    <button class="btn">Delete</button>
</form>
