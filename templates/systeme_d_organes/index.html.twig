{% extends 'base.html.twig' %}

{% block title %}SystemeDOrganes index{% endblock %}

{% block body %}
    <h1>SystemeDOrganes index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for systeme_d_organe in systeme_d_organes %}
            <tr>
                <td>{{ systeme_d_organe.id }}</td>
                <td>{{ systeme_d_organe.nom }}</td>
                <td>
                    <a href="{{ path('app_systeme_d_organes_show', {'id': systeme_d_organe.id}) }}">show</a>
                    <a href="{{ path('app_systeme_d_organes_edit', {'id': systeme_d_organe.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_systeme_d_organes_new') }}">Create new</a>
{% endblock %}
