{% extends 'base.html.twig' %}

{% block title %}ProfilDimensionnel index{% endblock %}

{% block body %}
    <h1>ProfilDimensionnel index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for profil_dimensionnel in profil_dimensionnels %}
            <tr>
                <td>{{ profil_dimensionnel.id }}</td>
                <td>{{ profil_dimensionnel.nom }}</td>
                <td>
                    <a href="{{ path('app_profil_dimensionnel_show', {'id': profil_dimensionnel.id}) }}">show</a>
                    <a href="{{ path('app_profil_dimensionnel_edit', {'id': profil_dimensionnel.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_profil_dimensionnel_new') }}">Create new</a>
{% endblock %}
