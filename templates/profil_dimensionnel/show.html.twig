{% extends 'base.html.twig' %}

{% block title %}ProfilDimensionnel{% endblock %}

{% block body %}
    <h1>ProfilDimensionnel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profil_dimensionnel.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ profil_dimensionnel.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_profil_dimensionnel_index') }}">back to list</a>

    <a href="{{ path('app_profil_dimensionnel_edit', {'id': profil_dimensionnel.id}) }}">edit</a>

    {{ include('profil_dimensionnel/_delete_form.html.twig') }}
{% endblock %}
