<form method="post" action="{{ path('app_profil_acu_meridien_delete', {'id': profil_acu_meridien.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profil_acu_meridien.id) }}">
    <button class="btn">Delete</button>
</form>
