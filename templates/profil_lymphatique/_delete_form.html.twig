<form method="post" action="{{ path('app_profil_lymphatique_delete', {'id': profil_lymphatique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profil_lymphatique.id) }}">
    <button class="btn">Delete</button>
</form>
