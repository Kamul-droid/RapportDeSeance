{% extends 'base.html.twig' %}

{% block title %}TableALanger{% endblock %}

{% block body %}
    <h1>TableALanger</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ table_a_langer.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ table_a_langer.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_table_a_langer_index') }}">back to list</a>

    <a href="{{ path('app_table_a_langer_edit', {'id': table_a_langer.id}) }}">edit</a>

    {{ include('table_a_langer/_delete_form.html.twig') }}
{% endblock %}
