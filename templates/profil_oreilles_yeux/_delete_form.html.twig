<form method="post" action="{{ path('app_profil_oreilles_yeux_delete', {'id': profil_oreilles_yeux.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profil_oreilles_yeux.id) }}">
    <button class="btn">Delete</button>
</form>
