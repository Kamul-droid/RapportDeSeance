<form method="post" action="{{ path('app_facteurs_miasmatiques_delete', {'id': facteurs_miasmatique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facteurs_miasmatique.id) }}">
    <button class="btn">Delete</button>
</form>
